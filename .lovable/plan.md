

## Plan: Center the "More Info" button in the Meal Train section

**File:** `src/components/sections/MealTrainSection.tsx` (line ~44-48)

**Change:** Replace the `mt-8` div wrapping the button with a centered container using `mt-8 flex justify-center`.

Single edit, no other changes needed.

